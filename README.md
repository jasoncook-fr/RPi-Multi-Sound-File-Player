# multiChannelSoundFilePlayer

dependencies:
sudo apt-get update
sudo apt-get install python3-pygame python3-pip

sudo pip3 install adafruit-circuitpython-mpr121

Activate the I2C port for communication with the mpr121
sudo raspi-config


